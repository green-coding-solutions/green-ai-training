---
name: Bottom Up Benchmark Python Conditionals - Simple If Statement
author: arne@green-coding.io
description: >
  Compare the energy cost of Python conditional dispatch styles.
  This scenario classifies 10M random integers from 1 to 10 using a 10-branch
  if-elif chain with equality checks.

custom_metrics:
  classifications:
    unit: classifications
    sci: True

services:
  python:
    image: python:3.15.0b1-slim-trixie

flow:
  - name: Classify with simple if-elif chain
    container: python
    commands:
      - type: console
        shell: bash
        command: python -B scripts/if_statement_simple.py
