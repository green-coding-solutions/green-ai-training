# Prompt

Optimize this file according to Green Coding Rules

# Expected Output

- Replace JavaScript-driven animation using `requestAnimationFrame` with inline style updates with CSS `@keyframes` animation, which is handled by the browser's compositor thread and requires significantly less CPU usage
- Expected energy saving when running this file 1.000 times
