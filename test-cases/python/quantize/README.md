# Prompt

Optimize this file according to Green Coding Rules

# Expected Output

- Decorate the quantization function with Numba's `@njit` to compile the pure Python loop iterating over a NumPy array to native machine code, eliminating interpreter overhead for each element
- Expected energy saving when running this file 1.000 times
