# Prompt

Optimize this file according to Green Coding Rules

# Expected Output

- Replace `list.insert(0, ...)` with `list.append()` to avoid O(n) element shifting on every insertion
- Expected energy saving when running this file 1.000 times
