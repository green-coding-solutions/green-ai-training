# Prompt

Optimize this file according to Green Coding Rules

# Expected Output

- Replace incremental string concatenation with `+` by collecting parts in a list and joining with `str.join()` to avoid creating a new string object on every iteration
- Expected energy saving when running this file 1.000 times
