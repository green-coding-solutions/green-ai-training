# Prompt

Optimize this file according to Green Coding Rules

# Expected Output

- Replace `case _ if condition:` guard patterns in match statements with direct value match cases (`case value:`) where possible, allowing Python to use optimized internal dispatch instead of evaluating guards sequentially
- Expected energy saving when running this file 1.000 times
